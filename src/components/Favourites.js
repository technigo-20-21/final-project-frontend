import React, { useEffect } from "react";

export const Favourites = ({ user }) => {
  console.log({ user });
  console.log(user.id);

  

  return (
    <>
      <h3>Mina favoriter</h3>
    </>
  );
};
